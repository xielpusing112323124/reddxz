import { defineCloudflareConfig } from '@opennextjs/cloudflare';

export default defineCloudflareConfig({
    /*
     * If you need to override the default configuration, you can do so here.
     * For example, you can set the `incrementalCache` option to `false` to disable the incremental cache.
     */
});
